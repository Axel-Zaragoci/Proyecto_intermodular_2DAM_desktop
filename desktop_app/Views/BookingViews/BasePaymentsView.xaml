<UserControl x:Class="desktop_app.Views.BookingViews.BasePaymentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:desktop_app.Views.BookingViews"
             mc:Ignorable="d"
             xmlns:viewmodels="clr-namespace:desktop_app.ViewModels"
             Style="{StaticResource UserControlBase}"
             DataContext="{x:Static viewmodels:BasePaymentsViewModel.Instance}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Row="0" Grid.Column="0" Margin="0 0 22 0">
            <Label Content="Método de pago: " Target="PaymentMethodComboBox" Style="{StaticResource LabelForm}"/>
            <ComboBox x:Name="PaymentMethodComboBox" ItemsSource="{Binding PaymentMethods}" Style="{StaticResource DropdownStyle}" SelectedValue="{Binding SelectedPaymentMethod}"/>
        </StackPanel>
        
        <ContentControl Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Content="{Binding CurrentView}" Margin="0 10 0 0"/>
    </Grid>
</UserControl>
