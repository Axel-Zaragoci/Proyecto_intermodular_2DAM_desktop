<UserControl x:Class="desktop_app.Views.NotificationsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewmodels="clr-namespace:desktop_app.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="450" 
             d:DesignWidth="800"
             Style="{StaticResource UserControlBase}">
    <UserControl.DataContext>
        <viewmodels:NotificationsViewModel/>
    </UserControl.DataContext>
    
    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="0.2*"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <Border Style="{StaticResource HeaderWrapper}">
            <DockPanel LastChildFill="False" VerticalAlignment="Center">
                <TextBlock Style="{StaticResource HeaderMainTitle}" DockPanel.Dock="Left" Text="Recordatorios"/>
            </DockPanel>
        </Border>
        
        <StackPanel Grid.Row="1" HorizontalAlignment="Right" Margin="0 0 0 10">
            <Button Style="{StaticResource SaveBtn}" Content="Recargar" Command="{Binding ReloadBookingsCommand}"/>
        </StackPanel>
        
        
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <StackPanel Grid.Column="0" Margin="0 0 22 0">
                <TextBlock Text="Próximas 24 horas" Style="{StaticResource NotificationListTitle}"/>
                <ItemsControl ItemsSource="{Binding Bookings24H}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource NotificationBorder}">
                                <Grid Margin="10">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="0.7*"/>
                                        <ColumnDefinition Width="0.3*"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <StackPanel Grid.Column="0">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="Habitación" Margin="0 0 5 0" Style="{StaticResource NotificationBigText}"/>
                                            <TextBlock Text="{Binding RoomNumber}" Style="{StaticResource NotificationBigText}"/>
                                        </StackPanel>
                                        
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding ClientName}" Style="{StaticResource NotificationText}"/>
                                            <TextBlock Text="-" Margin="5 0" Style="{StaticResource NotificationText}"/>
                                            <TextBlock Text="{Binding ClientDni}" Style="{StaticResource NotificationText}"/>
                                        </StackPanel>
                                    </StackPanel>
                                    
                                    <Grid Grid.Column="1">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width=".3*"/>
                                            <ColumnDefinition Width=".7*"/>
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height=".3*"/>
                                            <RowDefinition Height=".7*"/>
                                        </Grid.RowDefinitions>
                                        
                                        <TextBlock Grid.Row="0" Grid.Column="0" Grid.RowSpan="2" Text="schedule" Style="{StaticResource NotificationIcon}"/>
                                        
                                        <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal">
                                            <TextBlock Text="{Binding CheckInDate.Day}"  Style="{StaticResource NotificationBigText}"/>
                                            <TextBlock Text="/" Style="{StaticResource NotificationBigText}"/>
                                            <TextBlock Text="{Binding CheckInDate.Month}" Style="{StaticResource NotificationBigText}"/>
                                            <TextBlock Text="/" Style="{StaticResource NotificationBigText}"/>
                                            <TextBlock Text="{Binding CheckInDate.Year}" Style="{StaticResource NotificationBigText}"/>
                                        </StackPanel>
                                        
                                        <StackPanel  Grid.Row="1" Grid.Column="1" Orientation="Horizontal">
                                            <TextBlock Text="{Binding CheckInDate.Hour}" Margin="16 0 0 0" Style="{StaticResource NotificationText}"/>
                                            <TextBlock Text=":" Style="{StaticResource NotificationText}"/>
                                            <TextBlock Text="{Binding CheckInDate.Minute}" Style="{StaticResource NotificationText}"/>
                                            <TextBlock Text=":" Style="{StaticResource NotificationText}"/>
                                            <TextBlock Text="{Binding CheckInDate.Second}" Style="{StaticResource NotificationText}"/>
                                        </StackPanel>
                                    </Grid>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
            
            <StackPanel Grid.Column="1">
                <TextBlock Text="Próximas 48 horas" Style="{StaticResource NotificationListTitle}"/>
                <ItemsControl ItemsSource="{Binding Bookings48H}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource NotificationBorder}">
                                <Grid Margin="10">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="0.7*"/>
                                        <ColumnDefinition Width="0.3*"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <StackPanel Grid.Column="0">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="Habitación" Margin="0 0 5 0" Style="{StaticResource NotificationBigText}"/>
                                            <TextBlock Text="{Binding RoomNumber}" Style="{StaticResource NotificationBigText}"/>
                                        </StackPanel>
                                        
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding ClientName}" Style="{StaticResource NotificationText}"/>
                                            <TextBlock Text="-" Margin="5 0" Style="{StaticResource NotificationText}"/>
                                            <TextBlock Text="{Binding ClientDni}" Style="{StaticResource NotificationText}"/>
                                        </StackPanel>
                                    </StackPanel>
                                    
                                    <Grid Grid.Column="1">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width=".3*"/>
                                            <ColumnDefinition Width=".7*"/>
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height=".3*"/>
                                            <RowDefinition Height=".7*"/>
                                        </Grid.RowDefinitions>
                                        
                                        <TextBlock Grid.Row="0" Grid.Column="0" Grid.RowSpan="2" Text="schedule" Style="{StaticResource NotificationIcon}"/>
                                        
                                        <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal">
                                            <TextBlock Text="{Binding CheckInDate.Day}"  Style="{StaticResource NotificationBigText}"/>
                                            <TextBlock Text="/" Style="{StaticResource NotificationBigText}"/>
                                            <TextBlock Text="{Binding CheckInDate.Month}" Style="{StaticResource NotificationBigText}"/>
                                            <TextBlock Text="/" Style="{StaticResource NotificationBigText}"/>
                                            <TextBlock Text="{Binding CheckInDate.Year}" Style="{StaticResource NotificationBigText}"/>
                                        </StackPanel>
                                        
                                        <StackPanel  Grid.Row="1" Grid.Column="1" Orientation="Horizontal">
                                            <TextBlock Text="{Binding CheckInDate.Hour}" Margin="16 0 0 0" Style="{StaticResource NotificationText}"/>
                                            <TextBlock Text=":" Style="{StaticResource NotificationText}"/>
                                            <TextBlock Text="{Binding CheckInDate.Minute}" Style="{StaticResource NotificationText}"/>
                                            <TextBlock Text=":" Style="{StaticResource NotificationText}"/>
                                            <TextBlock Text="{Binding CheckInDate.Second}" Style="{StaticResource NotificationText}"/>
                                        </StackPanel>
                                    </Grid>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
