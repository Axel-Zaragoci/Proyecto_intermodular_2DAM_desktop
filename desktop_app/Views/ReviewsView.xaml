<UserControl x:Class="desktop_app.Views.ReviewsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             Style="{StaticResource UserControlBase}">

    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- HEADER -->
        <Border Grid.Row="0" Style="{StaticResource HeaderWrapper}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock Text="{Binding Room.RoomNumber, StringFormat='Reseñas - Habitación {0}'}" Style="{StaticResource HeaderMainTitle}"/>
                    <TextBlock Text="{Binding Room.Type, StringFormat='Tipo: {0}'}" Style="{StaticResource UpdateIdStyle}"/>
                </StackPanel>

                <Button Grid.Column="1"
                        Style="{StaticResource HeaderNavigateButton}"
                        Content="Volver"
                        Command="{Binding CancelCommand}" Width="70" Cursor="Hand"/>
            </Grid>
        </Border>

        <!-- CONTENIDO -->
        <Border Grid.Row="1" Style="{StaticResource FormBorder}">
            <Grid>
                <!-- Mensaje vacío -->
                <TextBlock Text="No hay reseñas para esta habitación"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Foreground="#9CA3AF"
                           FontSize="16">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsEmpty}" Value="True">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <!-- Lista de reseñas -->
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding Reviews}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="White" 
                                        CornerRadius="10" 
                                        Padding="15" 
                                        Margin="0,0,0,12"
                                        BorderBrush="#E5E7EB"
                                        BorderThickness="1">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>

                                        <!-- Usuario, fecha y botón eliminar -->
                                        <Grid Grid.Row="0">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <TextBlock Text="{Binding UserName}" 
                                                       FontWeight="SemiBold" 
                                                       FontSize="14"
                                                       Foreground="#1F3B4D"/>

                                            <TextBlock Grid.Column="1" 
                                                       Text="{Binding CreatedAt, StringFormat=d}" 
                                                       Foreground="#9CA3AF" 
                                                       FontSize="12"
                                                       VerticalAlignment="Center"
                                                       Margin="0,0,15,0"/>

                                            <!-- Botón eliminar -->
                                            <Button Grid.Column="2"
                                                    Content="delete"
                                                    Style="{StaticResource DeleteBookingBtn}"
                                                    Command="{Binding DataContext.DeleteReviewCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                    CommandParameter="{Binding}"/>
                                        </Grid>

                                        <!-- Estrellas -->
                                        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,8,0,8">
                                            <TextBlock Text="★" FontSize="16" Foreground="#FFC107"/>
                                            <TextBlock Text="{Binding Rating, StringFormat=' {0}/5'}" 
                                                       Foreground="#6B7280" 
                                                       FontWeight="SemiBold"
                                                       FontSize="14"/>
                                        </StackPanel>

                                        <!-- Descripción -->
                                        <TextBlock Grid.Row="2"
                                                   Text="{Binding Description}" 
                                                   TextWrapping="Wrap" 
                                                   Foreground="#374151"
                                                   FontSize="13"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</UserControl>
