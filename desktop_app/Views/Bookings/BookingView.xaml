<UserControl x:Class="desktop_app.Views.BookingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:desktop_app.Views"
             xmlns:viewmodels="clr-namespace:desktop_app.ViewModels.Booking"
             mc:Ignorable="d" 
             d:DesignHeight="450" 
             d:DesignWidth="800"
             Style="{StaticResource UserControlBase}">
    <UserControl.DataContext>
        <viewmodels:BookingViewModel/>
    </UserControl.DataContext>
    
    <Grid Background="Transparent"  Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        
        <Border Style="{StaticResource HeaderWrapper}">
            <DockPanel LastChildFill="False" VerticalAlignment="Center">
                <Border Background="#F7FAFD" CornerRadius="14" Padding="12" Margin="0 14 0 0" DockPanel.Dock="Bottom">
                    <StackPanel Orientation="Horizontal">
                        <StackPanel Margin="0 0 20 0">
                            <Label Content="Cliente" Style="{StaticResource LabelForm}"/>
                            <TextBox Width="100" Text="{Binding FilterName, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TextBoxForm}"/>
                        </StackPanel>
                        
                        <StackPanel Margin="0 0 20 0">
                            <Label Content="Habitación" Style="{StaticResource LabelForm}"/>
                            <TextBox Width="100" Text="{Binding FilterRoom, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TextBoxForm}"/>
                        </StackPanel>
                        
                        <StackPanel Margin="0 0 20 0">
                            <Label Content="Estado" Style="{StaticResource LabelForm}"/>
                            <ComboBox Width="100" ItemsSource="{Binding Statuses}" SelectedItem="{Binding SelectedStatus}" Style="{StaticResource DropdownStyle}"/>
                        </StackPanel>
                        
                        <StackPanel Margin="0 0 20 0">
                            <Label Content="Fecha de inicio" Style="{StaticResource LabelForm}"/>
                            <DockPanel>
                                <DatePicker SelectedDate="{Binding SelectedStartDate, Mode=TwoWay}" Style="{StaticResource DatePickerForm}" DockPanel.Dock="Left" Margin="0 0 10 0"/>
                                <ComboBox ItemsSource="{Binding DateFilterTypes}" SelectedItem="{Binding SelectedStartDateFilter}" Style="{StaticResource DropdownStyle}" DockPanel.Dock="Right"/>
                            </DockPanel>
                        </StackPanel>
                        
                        <StackPanel>
                            <Label Content="Fecha de fin" Style="{StaticResource LabelForm}"/>
                            <DockPanel>
                                <DatePicker SelectedDate="{Binding SelectedEndDate, Mode=TwoWay}" Style="{StaticResource DatePickerForm}" DockPanel.Dock="Left" Margin="0 0 10 0"/>
                                <ComboBox ItemsSource="{Binding DateFilterTypes}" SelectedItem="{Binding SelectedEndDateFilter}" Style="{StaticResource DropdownStyle}" DockPanel.Dock="Right"/>
                            </DockPanel>
                        </StackPanel>
                    </StackPanel>
                </Border>
                
                <TextBlock Style="{StaticResource HeaderMainTitle}" DockPanel.Dock="Left" Text="Reservas"/>
            
                <Button Style="{StaticResource HeaderNavigateButton}" Content="+  Crear reserva" DockPanel.Dock="Right" Command="{Binding CreateBookingCommand}"/>
                
            </DockPanel>
        </Border>
        
        <StackPanel Grid.Row="1" HorizontalAlignment="Right" Margin="0 0 0 10">
            <Button Style="{StaticResource SaveBtn}" Content="Recargar" Command="{Binding ReloadBookingCommand}"/>
        </StackPanel>
        
        <ListView Grid.Row="2" ItemsSource="{Binding Bookings}" SizeChanged="ListView_SizeChanged" AlternationCount="2" ItemContainerStyle="{StaticResource AlternatingListViewItemStyle}" Margin="10 3" BorderThickness="0.5" ScrollViewer.CanContentScroll="True">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Habitación" HeaderContainerStyle="{StaticResource TableHeader}">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                    <TextBlock Text="{Binding RoomNumber}" Style="{StaticResource GridViewCellText}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    
                    <GridViewColumn Header="Cliente" HeaderContainerStyle="{StaticResource TableHeader}">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                    <TextBlock Text="{Binding ClientName}" Style="{StaticResource GridViewCellText}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    
                    <GridViewColumn Header="Fecha de inicio" HeaderContainerStyle="{StaticResource TableHeader}">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding CheckInDate, StringFormat='dd/MM/yyyy'}" Style="{StaticResource GridViewCellText}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    
                    <GridViewColumn Header="Fecha de fin" HeaderContainerStyle="{StaticResource TableHeader}">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding CheckOutDate, StringFormat='dd/MM/yyyy'}" Style="{StaticResource GridViewCellText}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    
                    <GridViewColumn Header="Fecha de pago" HeaderContainerStyle="{StaticResource TableHeader}">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding PayDate, StringFormat='dd/MM/yyyy'}" Style="{StaticResource GridViewCellText}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    
                    <GridViewColumn Header="Total de noches" HeaderContainerStyle="{StaticResource TableHeader}">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding TotalNights}" Style="{StaticResource GridViewCellText}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    
                    <GridViewColumn Header="Estado" HeaderContainerStyle="{StaticResource TableHeader}">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Border Style="{StaticResource StatusTextStyle}">
                                    <TextBlock Text="{Binding Status}" Style="{StaticResource GridViewCellText}" Foreground="#ffffff"/>
                                </Border>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    
                    <GridViewColumn Header="" HeaderContainerStyle="{StaticResource TableHeader}" Width="65">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Button Style="{StaticResource UpdateBookingBtn}" Content="edit" Command="{Binding DataContext.EditBookingCommand, RelativeSource={RelativeSource AncestorType=ListView}}" CommandParameter="{Binding}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    
                    <GridViewColumn Header="" HeaderContainerStyle="{StaticResource TableHeader}" Width="65">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Button Style="{StaticResource DeleteBookingBtn}" Content="delete" Command="{Binding DataContext.DeleteBookingCommand, RelativeSource={RelativeSource AncestorType=ListView}}" CommandParameter="{Binding}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
        
        <Border Grid.Row="3" VerticalAlignment="Bottom" Margin="10 10 10 15" Background="Transparent">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal" Margin="0 0 20 0">
                    <TextBlock Text="Mostrar:" VerticalAlignment="Center" Margin="0 0 8 0"/>
                    <ComboBox ItemsSource="{Binding PageSizeOptions}" SelectedItem="{Binding PageSize}" Style="{StaticResource DropdownStyle}"/>
                    <TextBlock Text="reservas" VerticalAlignment="Center" Margin="8 0 0 0"/>
                </StackPanel>
                
                <Button Content="1" Command="{Binding FirstPageCommand}" Style="{StaticResource NavigationBtn}" IsEnabled="{Binding HasPreviousPage}"/>
        
                <Button Content="«" Command="{Binding PreviousPageCommand}"  Style="{StaticResource NavigationBtn}"  IsEnabled="{Binding HasPreviousPage}"/>
                
                <Border Background="#F7FAFD" CornerRadius="4" Padding="8 4" Margin="0 0 8 0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Página " VerticalAlignment="Center"/>
                        <TextBox Text="{Binding CurrentPage, UpdateSourceTrigger=PropertyChanged}" 
                                 TextAlignment="Center"
                                 Margin="4 0"
                                 Style="{StaticResource TextBoxForm}"/>
                        <TextBlock Text="{Binding TotalPages}" VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>
                
                <Button Content="»" Command="{Binding NextPageCommand}" Style="{StaticResource NavigationBtn}" IsEnabled="{Binding HasNextPage}"/>
        
                <Button Content="{Binding TotalPages}" Command="{Binding LastPageCommand}" Style="{StaticResource NavigationBtn}" IsEnabled="{Binding HasNextPage}"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
