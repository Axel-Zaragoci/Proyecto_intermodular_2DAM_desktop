<UserControl x:Class="desktop_app.Views.Bookings.RefundView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:desktop_app.Views.Bookings"
             xmlns:viewModels="clr-namespace:desktop_app.ViewModels.Booking"
             mc:Ignorable="d"
             Style="{StaticResource UserControlBase}"
             DataContext="{x:Static viewModels:RefundViewModel.Instance}">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        
        <Label Grid.Row="0" Content="Razón del reembolso: " Target="ReasonTextBox" Style="{StaticResource LabelForm}" HorizontalAlignment="Left"/>
        <TextBox Grid.Row="1" x:Name="ReasonTextBox" Text="{Binding Reason}" Style="{StaticResource TextBoxForm}" Width="500" HorizontalAlignment="Left"/>
        <Button Grid.Row="2" Content="Reembolsar" Style="{StaticResource SaveBtn}" Margin="0 30 0 0" Command="{Binding RefundCommand}" Width="110" HorizontalAlignment="Left"/>
    </Grid>
</UserControl>
