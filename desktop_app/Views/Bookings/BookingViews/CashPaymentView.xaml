<UserControl x:Class="desktop_app.Views.BookingViews.CashPaymentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewmodels="clr-namespace:desktop_app.ViewModels.Booking"
             xmlns:local="clr-namespace:desktop_app.Views.BookingViews"
             mc:Ignorable="d"
             Style="{StaticResource UserControlBase}"
             DataContext="{x:Static viewmodels:CashPaymentsViewModel.Instance}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0" Grid.Column="0" Margin="0 0 22 0">
            <Label Content="Precio a pagar: " Target="PricePaidTextBox" Style="{StaticResource LabelForm}"/>
            <TextBox x:Name="PricePaidTextBox" Text="{Binding PricePaid, Mode=TwoWay}" Style="{StaticResource TextBoxForm}"/>
        </StackPanel>
        
        <StackPanel Grid.Row="1" Grid.Column="0" Margin="0 0 22 0">
            <Label Content="Efectivo entregado: " Target="AmountReceivedTextBox" Style="{StaticResource LabelForm}"/>
            <TextBox x:Name="AmountReceivedTextBox" Text="{Binding AmountReceived, Mode=TwoWay}" Style="{StaticResource TextBoxForm}"/>
        </StackPanel>
        
        <StackPanel Grid.Row="0" Grid.Column="1">
            <Label Content="Total a pagar de la reserva: " Target="TotalToPayTextBox" Style="{StaticResource LabelForm}"/>
            <TextBox x:Name="TotalToPayTextBox" Text="{Binding TotalToPay, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TextBoxForm}" IsEnabled="False"/>
        </StackPanel>
        
        <StackPanel Grid.Row="1" Grid.Column="1">
            <Label Content="Cambio a dar: " Target="ChangeGivenTextBox" Style="{StaticResource LabelForm}"/>
            <TextBox x:Name="ChangeGivenTextBox" Text="{Binding ChangeGiven, Mode=OneWay}" Style="{StaticResource TextBoxForm}" IsEnabled="False"/>
        </StackPanel>
        
        <StackPanel Grid.Row="2" Grid.Column="0" Orientation="Horizontal" Margin="0 30 0 0">
            <Button Content="Guardar pago" Style="{StaticResource SaveBtn}" Margin="0 0 30 0" Command="{Binding SaveCommand}"/>
        </StackPanel>    
    </Grid>
</UserControl>
